<?php

/*
//writing file
$file = "72_file_new.txt";
$handle = fopen($file,'w');
$write = fwrite($handle,'original text');
fclose($handle);


//appending file
$file = "72_file_new.txt";
$handle = fopen($file,'a');
$write = fwrite($handle,'appending text');
fclose($handle);


//include and include_once
include -> can call anywhere and it loads
include_once -> can call once and it loads once
It works continue even if the statements  are wrong

////require and require_once
require -> can call anywhere and it loads
require_once -> can call once and it loads once
It doesn't work continue if the statements  are wrong
*/

?>
